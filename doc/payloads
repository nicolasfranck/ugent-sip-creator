TODO
    file checks: (met ignore mogelijkheid)
        wat eruit ziet als jpg, moet ook jpeg zijn
            op basis van extensie
            op basis van magic code (kan wijzen jpeg, terwijl de rest van de data daaraan niet beantwoordt)

        http://www.rgagnon.com/javadetails/java-0487.html
        http://stackoverflow.com/questions/51438/getting-a-files-mime-type-in-java

        => standaard (traag en beperkte mime-types): op basis van extensie

            java.io.File:
                new File().toURL().openConnection().getContentType();
            java.net.*:
                URLConnection.guessContentTypeFromName(file.getName()) 

        => JAF API (jdk 6):
            javax.activation: leidt mimetype af op basis van extensie

        => MimeUtil
            lightweight
            fast
            1 dependency: slf4j

            vb. test.tar                

            <MimeType>.getMediaType() vb. 'application'
            <MimeType>.getSubType() vb. 'tar'

        => Apache Tika
            hoge depency graad (20 jars!)

        => JMimeMagic (gebruikt org.apache.oro.text.perl.Perl5Util!)
            test File of Byte [] op extensie door magic headers te lezen

            gooit exceptions bij eerste falen
            kent weinig mimetypes (vb image/* kent hij niet)

        => Droid

            => CLI of BUI (geen library..pff)

        => Aperture framework

            http://sourceforge.net/apps/trac/aperture/wiki/Overview
            veelbelovend, maar herkent vb. geen image/jp2
